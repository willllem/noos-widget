<%= form_with(model: wearable) do |form| %>
  <% if wearable.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wearable.errors.count, "error") %> prohibited this wearable from being saved:</h2>

      <ul>
        <% wearable.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
